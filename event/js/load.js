

function setup(){
    let textInput;
    textInput=document.getElementById('username');
    textInput.focus();//blur의 반대 포커스 선택, blur는 벗어나면
}
window.addEventListener('load', setup, false);
//load는 페이지가 html이나 이미지 css 스크립트 등 로드를 모두 완료하면
//window객체에 의해 자동으로 발생한다.